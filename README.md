# Semantic Modeling

This project demonstrates semantic modeling with and without a semantic layer.

## Files

*   `app.py`: Main application file.
*   `with_semantic.py`: Example with a semantic layer.
*   `without_semantic.py`: Example without a semantic layer.
*   `dbt_project/`: dbt project for data transformation.
